<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trendie"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:ignore="ExtraText">

        <!-- 앱 시작점 (런처) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 로그인 (딥링크 콜백 수신) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <!-- 앱 런처 인텐트는 SplashActivity 쪽에만 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <!-- fastapi_test.py에서 RedirectResponse가 보낸 스킴/호스트와 동일해야 함 -->
                <data android:scheme="trendie" android:host="oauth-callback"/>
            </intent-filter>
        </activity>

        <!-- 신규 가입 플로우 -->
        <activity
            android:name=".NicknameActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name=".PreferenceActivity"
            android:exported="false" />

        <!-- 닉네임 수정 -->
        <activity
            android:name=".NicknameEditActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- 메인 화면 (Fragment 호스팅) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 설정/정보/기록 화면들 -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".FeedbackHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".FeedbackReportActivity"
            android:exported="false" />
        <activity
            android:name=".PreferenceHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".MemberInfoActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>