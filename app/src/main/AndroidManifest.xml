<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trendie"
        tools:ignore="ExtraText"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 앱 시작점 -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 로그인 / 초기 플로우 -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:launchMode="singleTop">

            <!-- trendie://oauth/callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="trendie"
                    android:host="oauth"
                    android:path="/callback" />
            </intent-filter>
        </activity>

        <activity android:name=".NicknameActivity" android:exported="true"
                  android:windowSoftInputMode="adjustResize" />
        <activity android:name=".PreferenceActivity" android:exported="true" />

        <!-- 닉네임 수정 -->
        <activity android:name=".NicknameEditActivity" android:exported="true" />

        <!-- 메인 화면 (Fragment 탑재용) -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- 설정 화면 -->
        <activity android:name=".SettingsActivity" android:exported="true" />

        <activity android:name=".FeedbackHistoryActivity"
                  android:exported="true"/>

        <!-- 피드백 업로드 -->
        <activity android:name=".FeedbackUploadFragment"     android:exported="true"
            tools:ignore="Instantiatable" />

        <!-- 피드백 기록 -->
        <activity android:name=".FeedbackReportActivity"  android:exported="true" />

        <!-- 선호도 조사 내역 -->
        <activity android:name=".PreferenceHistoryActivity"/>

        <!-- 가입 정보 -->
        <activity android:name=".MemberInfoActivity"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>